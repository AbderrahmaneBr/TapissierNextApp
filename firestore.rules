rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true; // Allow read access to all documents
      
      // Allow write access without authentication
      allow write: if true;
    }
  }
}